# 샘플 문서

`npm run seed` 이 이 폴더의 프로젝트들을 작업 폴더로 복사합니다.

샘플이 **이 프로젝트가 정의하는 포맷 그 자체로** 저장되어 있다는 점이 핵심입니다.
Rust 리터럴이나 생성 스크립트가 아니라, 슬라이드마다 `.md` + `.layout.json` 한 쌍입니다.
샘플을 고치려면 마크다운을 고치면 됩니다. `manifest.json`의 항목 목록과 `AI.md`는
복사 후 다시 저장하면서 자동으로 재생성되므로 여기에는 두지 않습니다.
