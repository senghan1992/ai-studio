{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the AI Studio window is allowed to do. Documents are reached through this crate's own commands, so no filesystem plugin is granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:allow-save",
    "dialog:allow-message",
    "opener:allow-reveal-item-in-dir"
  ]
}
